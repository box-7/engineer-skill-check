<div class="profile_space">
  <p class="employee_index"><%= link_to '一覧に戻る', articles_path, class: 'btn employee_index' %></p>
  <div class="row">
    <div>
      <span class="label">タイトル</span>
      <span><%= @article.title %></span>
    </div>
    <span class="profile_value linefeed"><%= @article.content %></span>
  </div>
</div>
